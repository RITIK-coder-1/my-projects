const add = (num1, num2) => {
    console.log(num1 + num2);
    return num1 + num2
}

const sub = (num1, num2) => {
    console.log(num1 - num2);
    return num1 - num2
}

const multiply = (num1, num2) => {
    console.log(num1 * num2);
    return num1 * num2
}

const division = (num1, num2) => {
    console.log(num1 / num2);
    return num1 / num2
}