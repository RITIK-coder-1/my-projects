// Implement a program that prompts the user for two numbers and swaps their values --->

function swap(value1, value2) {
    return `${value2},${value1}`
}

const result = swap(1, 3)
console.log(result);